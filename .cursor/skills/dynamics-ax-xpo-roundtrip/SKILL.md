---
name: dynamics-ax-xpo-roundtrip
description: Пайплайн XPO ↔ XPP (parserXPO), кодировки, флаги CI. Перед любыми правками .xpp обязательно применять правила из .cursor/rules/comment_rules.mdc и commentmeta.json.
---

# Dynamics AX XPO roundtrip

## Обязательный порядок (один запуск скилла = один чеклист)

Пока этот скилл активен по задаче XPO/X++, **не редактируй** файлы `parserXPO/**/*.xpp`, пока не выполнено **оба** шага (в таком порядке):

1. **Прочитай** `@.cursor/rules/comment_rules.mdc` (инструмент чтения файла — обязателен; полагаться только на «правило уже в контексте» нельзя).
2. **Прочитай** `commentmeta.json` в корне репозитория для `developer` и `project` (дату в комментариях брать **сегодня**, см. `comment_rules`).

Этот скилл **не заменяет** `comment_rules.mdc`: он только добавляет пайплайн XPO. Любое изменение `.xpp` оформляй **строго** по `comment_rules.mdc` (включая блоки `// +` / `// -`, исключения для классов/методов из того же файла).

Если `comment_rules.mdc` недоступен — остановись и попроси пользователя; **не** правь `parserXPO` «вслепую».

## Источник правды

- Редактируемый X++ только в `parserXPO/**/*.xpp` (и при необходимости `properties.txt` в каталоге объекта).
- Не выгружать и не вставлять «весь AOT» из огромного CUS-экспорта без индекса или MCP.

## Скрипты

- Разбор XPO → дерево: `xpo_parser.py` → `parserXPO/<Object>/<Method>.xpp`, UTF-8.
- Сборка обратно: `xpo_writer.py` → рядом с исходным XPO файл `<stem>_WR.xpo`.
- Точечное извлечение по SQLite: `parse_object_from_index(...)` в `xpo_parser.py`, БД `indexXPO_cus/xpo_index.db`, таблица `elements`.

## Флаги

- `--force` (parser): перезапись существующих `.xpp`. (writer): игнорировать сравнение по mtime/равенству текста.
- `--no-input` или `CI=1` / `XPO_NO_INPUT=1`: без диалогов; при нескольких `.xpo` в каталоге `XPO` без явного пути — обработать все; writer не спрашивает про добавление элемента по шаблону.

## Кодировки

- Импорт в AOT: XPO в **Windows-1251 (cp1251)**; `xpo_writer` сохраняет в кодировке исходного файла.
- `parserXPO`: UTF-8.

## После правок

После изменения `.xpp` предложи запуск `xpo_writer.py` и укажи ожидаемый путь `*_WR.xpo`.
